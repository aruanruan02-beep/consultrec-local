# 本地咨询录音处理系统：Codex 项目指令

本文件是本仓库的持久项目指令。详细的部署流程与系统背景见 `README.md`；执行部署或修改前先阅读它。

## 隐私与安全（最高优先级）

- 录音、逐字稿、临床记录与 `data/` 内容只能留在使用者本机。不得上传、复制到聊天、提交 Git，或暴露到局域网/公网。
- 服务只能绑定 `127.0.0.1`，不得改为 `0.0.0.0`。
- 不读取、显示、索取、传输或提交 Hugging Face token。若自动说话人区分需要 token，要求使用者自行在本机安全保存。
- 不提交 `.env`、`config/local_settings.json`、`data/`、`测试数据/`、`.venv/`、`.pyannote-venv/`、`node_modules/` 或音视频文件。
- 模型生成内容仅作本地辅助整理；不应被表述为诊断、治疗建议或专业判断。

## 部署任务

当用户要求部署或启动项目时：

1. 先阅读 `README.md`，检查 Apple 芯片 Mac、至少 20 GB 可用空间、Python、Ollama、项目文件和网络状态。
2. 执行 README 中的部署流程；涉及安装软件、下载模型或改变系统状态时，先向用户说明并请求确认。
3. 除非用户明确要求并已完成 Hugging Face 授权，否则不要启用自动说话人区分。
4. 不要用真实录音验证部署。只验证本地服务可在 `http://127.0.0.1:8000` 访问。
5. 完成后说明已安装组件、本地地址、如何停止服务，以及仍需用户完成的授权事项。

## 代码库约定

- 前端：Vite + React，源文件在 `web/src/`，构建产物在 `web/static/`；使用 Arco Design，样式在 `web/src/styles.css`。
- 后端：FastAPI，接口在 `consultrec/web.py`，处理编排在 `consultrec/processor.py`。
- 运行数据位于 `data_root`，由用户配置；不要将此目录纳入版本控制。
- 临床记录 JSON 的字段是动态的：后端不要强制固定 key；前端以 `Object.keys()` 渲染。
- 编辑说话人角色时，保留单段修改与“批量修改所有同名角色”的选项；角色列表应根据全文现有角色动态计算。

## 验证

- 修改 Python 逻辑后，运行相关 `tests/` 测试。
- 修改前端后，运行 `pnpm build`，并确保构建产物与源码同步。
- 保持改动聚焦；不要修改或删除用户的本机数据与配置。
